---
- hosts: "{{ hostvar }}"
  gather_facts: true
  become: false
  strategy: free

  tasks:

    - debug:
        msg: "{{ ansible_date_time.time }}"

    - shell:
        cmd: sleep $((RANDOM % 300 + 0))

    - debug:
        msg: "Done"

# - hosts: "{{ hostvar }}"
#   gather_facts: true
#   become: false
#   strategy: free

#   tasks:
   
#     - debug:
#         msg: "{{ ansible_date_time.time }}"
        