---
- name: Run first task
  hosts: "{{ hostvar }}"
  gather_facts: false
  become: true

  tasks:

    - name: sleep 1
      ansible.builtin.pause:
        seconds: 30

    - name: sleep 2
      ansible.builtin.pause:
        seconds: 30

    - name: sleep 3
      ansible.builtin.pause:
        seconds: 30
        
    - name: sleep 4
      ansible.builtin.pause:
        seconds: 30