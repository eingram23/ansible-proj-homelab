- name: Test
  hosts: "{{ hostvar }}"
  gather_facts: true

  tasks:

    - name: Run PowerShell script that modifies the module changed result
      ansible.windows.win_powershell:
        script: |
          get-service spooler