---
- name: Run first task
  hosts: "{{ hostvar }}"
  gather_facts: false
  become: true

  tasks:

    - name: sleep 1
      ansible.windows.win_shell: start-sleep 30

    - name: sleep 2
      ansible.windows.win_shell: start-sleep 30

    - name: sleep 3
      ansible.windows.win_shell: start-sleep 30

    - name: sleep 4
      ansible.windows.win_shell: start-sleep 30


        