---
- hosts: "{{ hostvar }}"
  gather_facts: true
  become: false
#   strategy: free

  tasks:

    - name: Display timestamp
      debug:
        msg: "{{ ansible_date_time.time }}"

    - name: Run shell command
      shell:
        cmd: sleep 10000

    - name: Display end message
      debug:
        msg: "Done"

# - hosts: "{{ hostvar }}"
#   gather_facts: true
#   become: false
#   strategy: free

#   tasks:
   
#     - debug:
#         msg: "{{ ansible_date_time.time }}"
        