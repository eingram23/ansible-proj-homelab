---
- name: Run first task
  hosts: "{{ hostvar }}"
  gather_facts: false
  become: false

  tasks:

    - block:
        - name: Test task
          command: /bin/true
          failed_when: true
      rescue:
        - debug: 
            msg: "rescue task"


        